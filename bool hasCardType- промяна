// показване на картите на играчите
void showHand(const vector<string>& hand) {
    // обхождаме ръката и показваме картите
    for (int i = 0; i < hand.size(); i++) {
        cout << hand[i] << " "; // показваме всяка карта
    }
    cout << endl; // нов ред след показване на картите
}
